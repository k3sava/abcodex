export const signalTypes = ["complaint", "workaround", "feature_request", "alternative_request", "switching_trigger", "pricing_complaint", "ux_complaint", "performance_complaint", "integration_complaint", "support_complaint", "trust_security", "adoption_friction", "emotional_frustration", "workflow_pain", "stack_fragmentation"] as const;
export type SignalType = typeof signalTypes[number];
export type SourceType = "reddit" | "hacker_news" | "product_hunt" | "app_store" | "youtube" | "csv" | "url" | "x_twitter" | "manual";
export type RawRecord = { id: string; projectId: string; source: SourceType; sourceUrl?: string; author?: string; date?: string; title?: string; body: string; product?: string; engagement?: number; rawPayload?: unknown; ingestedAt: string; };
export type Classification = { recordId: string; signalType: SignalType; painCategory: string; emotionalIntensity: number; urgency: number; persona: string; productArea: string; workflowStep: string; competitorsMentioned: string[]; toolsMentioned: string[]; workaroundPresent: boolean; willingnessToPaySignal: boolean; summary: string; quotableLanguage: string; confidence: number; why: string; };
export type Cluster = { id: string; projectId: string; name: string; description: string; recordIds: string[]; representativeQuotes: string[]; frequency: number; sourceMix: Record<string, number>; personas: string[]; productsMentioned: string[]; emotionalIntensity: number; opportunityScore: number; evidenceStrength: number; recommendedProductWedge: string; recommendedPmmAngle: string; };
export type Opportunity = { id: string; projectId: string; clusterId: string; title: string; targetUser: string; problemStatement: string; existingAlternatives: string[]; whyCurrentToolsFail: string; mvpScope: string[]; willingnessToPayHypothesis: string; pricingHypothesis: string; distributionChannels: string[]; validationPlan: string[]; risks: string[]; moatPossibilities: string[]; whyNow: string; consultingRelevance: string; buildRecommendation: "build" | "validate_first" | "avoid"; score: number; evidenceRecordIds: string[]; };
