import { PageHeader, ProjectTabs } from "@/components/ui";
export default function Sources({ params }: { params: { id: string } }) { const sourceTypes = ["Reddit query", "Hacker News query", "CSV import", "URL ingestion", "YouTube comments", "X/Twitter manual import", "G2/Capterra CSV", "App Store reviews"]; return <><PageHeader eyebrow="Source Manager" title="Add compliant public and user-provided sources." /><ProjectTabs id={params.id} /><div className="grid gap-4 md:grid-cols-2">{sourceTypes.map(s => <div className="card p-5" key={s}><h3 className="font-bold">{s}</h3><input className="input mt-3" placeholder="query, URL, product name, or CSV mapping" /><button className="btn-secondary mt-3">Add source</button></div>)}</div></>; }
