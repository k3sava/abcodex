import { Evidence, PageHeader, ProjectTabs } from "@/components/ui";
import { clusters } from "@/lib/demo-data";
export default function Clusters({ params }: { params: { id: string } }) { return <><PageHeader eyebrow="Clustering Engine" title="Recurring pain themes with deterministic opportunity scores." /><ProjectTabs id={params.id} /><div className="grid gap-5">{clusters.filter(c => c.projectId === params.id).map(c => <article className="card p-6" key={c.id}><div className="flex flex-wrap justify-between gap-4"><h2 className="text-2xl font-bold">{c.name}</h2><div className="text-3xl font-black text-teal-300">{c.opportunityScore}</div></div><p className="mt-2 text-slate-300">{c.description}</p><div className="mt-4 grid gap-3 md:grid-cols-3"><div><div className="label">Personas</div><p>{c.personas.join(", ")}</p></div><div><div className="label">Products</div><p>{c.productsMentioned.join(", ")}</p></div><div><div className="label">Source mix</div><p>{Object.entries(c.sourceMix).map(([k, v]) => `${k}: ${v}`).join(" · ")}</p></div></div><div className="mt-4 grid gap-3">{c.representativeQuotes.map(q => <Evidence key={q} quote={q} why="Representative quote within this semantic cluster." />)}</div><div className="mt-4 rounded-xl bg-violet-500/10 p-4"><b>Product wedge:</b> {c.recommendedProductWedge}<br/><b>PMM angle:</b> {c.recommendedPmmAngle}</div></article>)}</div></>; }
