import Link from "next/link";
export function Stat({ label, value, hint }: { label: string; value: string | number; hint?: string }) { return <div className="card p-5"><div className="label">{label}</div><div className="mt-2 text-3xl font-black">{value}</div>{hint && <p className="mt-1 text-sm text-slate-400">{hint}</p>}</div>; }
export function PageHeader({ eyebrow, title, children }: { eyebrow: string; title: string; children?: React.ReactNode }) { return <header className="mb-8"><div className="label">{eyebrow}</div><h1 className="mt-2 max-w-4xl text-4xl font-black tracking-tight md:text-6xl">{title}</h1>{children && <div className="mt-4 max-w-3xl text-slate-300">{children}</div>}</header>; }
export function ProjectTabs({ id }: { id: string }) { const tabs = [["Overview", `/projects/${id}`], ["Sources", `/projects/${id}/sources`], ["Raw Signals", `/projects/${id}/signals`], ["Clusters", `/projects/${id}/clusters`], ["Opportunities", `/projects/${id}/opportunities`], ["Reports", `/projects/${id}/reports`], ["Saved", `/projects/${id}/saved`]]; return <div className="mb-6 flex flex-wrap gap-2">{tabs.map(([label, href]) => <Link className="btn-secondary" key={href} href={href}>{label}</Link>)}</div>; }
export function Evidence({ quote, url, why }: { quote: string; url?: string; why?: string }) { return <div className="rounded-xl border border-teal-400/20 bg-teal-400/5 p-4"><blockquote className="text-sm text-slate-100">“{quote}”</blockquote>{url && <a className="mt-2 block text-xs text-teal-300 underline" href={url}>Source evidence</a>}{why && <p className="mt-2 text-xs text-slate-400">Why: {why}</p>}</div>; }
